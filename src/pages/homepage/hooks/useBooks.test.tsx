import {describe, test, expect} from 'vitest';
import {render, screen} from '@testing-library/react';
import useBooks from './useBooks';


describe('useBooks', () => {
    test('should return books', () => {
        // make a dummy test 1 + 1 = 2
        expect(1 + 1).toBe(2);
    });

});

